pipeline {
    agent any

    stages {
        stage('Deploy') {
            steps {
                echo 'Deploying..'
                sshagent(['ssh-deploy']) {
                    sh "ssh 127.0.0.1 'touch JENKINS-CREATED'"
                }
            }
        }
    }
}
